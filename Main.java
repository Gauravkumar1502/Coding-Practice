
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.Arrays;

import codingproblems.leetcode.Problem1;
import codingproblems.leetcode.Problem118;
import dsa.dataStructures.ArrayList;
import dsa.dataStructures.SinglyLinkedList;

public class Main {
        public static void main(String[] args) {
                int[] arr = { 5, 4, 3, 5, 4, 8, 9, 10, 11, 12 };
                // ArrayList arrayList = new ArrayList(2);
                // arrayList.add(1);
                // System.out.printf("length: %d - capacity: %d\n", arrayList.length(),
                // arrayList.capacity());
                // arrayList.add(2);
                // System.out.printf("length: %d - capacity: %d\n", arrayList.length(),
                // arrayList.capacity());
                // arrayList.add(1, 4);
                // System.out.printf("length: %d - capacity: %d\n", arrayList.length(),
                // arrayList.capacity());
                // arrayList.add(3);
                // System.out.printf("length: %d - capacity: %d\n", arrayList.length(),
                // arrayList.capacity());

                // arrayList.addAll(new int[] { 1, 2, 3, 4 });
                // System.out.printf("length: %d - capacity: %d\n", arrayList.length(),
                // arrayList.capacity());
                // System.out.println(arrayList);
                // arrayList.addAll(2, new int[] { 1, 2, 3, 4 });
                // System.out.printf("length: %d - capacity: %d\n", arrayList.length(),
                // arrayList.capacity());
                // arrayList.clear();
                // System.out.printf("length: %d - capacity: %d\n", arrayList.length(),
                // arrayList.capacity());
                // ArrayList arrayList2 = arrayList.clone();
                // System.out.println(arrayList.indexOf(2, 3, 3));
                // System.out.println(arrayList.remove(1));
                // arrayList.removeAll(4, 1, 3, 5);
                // Arrays.stream(arrayList.subList(1, 3)).forEach(System.out::println);
                // Arrays.stream(arrayList.toArray()).forEach(System.out::println);
                // arrayList.set(3, 4);
                // arrayList.sort(Order.DESCENDING);
                // arrayList.shuffle();
                // Arrays.stream(arrayList.retainAll(1, 3, 3, 2, 2,
                // 4)).forEach(System.out::println);
                // System.out.println(arrayList.containsAll(1, 2, 2));
                // arrayList.removeRange(1, 3);
                // System.out.printf("length: %d - capacity: %d\n", arrayList.length(),
                // arrayList.capacity());
                // System.out.println(arrayList);
                // SinglyLinkedList singlyLinkedList = new SinglyLinkedList();
                // singlyLinkedList.add(3);
                // singlyLinkedList.shift(0);
                // singlyLinkedList.add(0, -1);
                // singlyLinkedList.add(3, 2);
                // singlyLinkedList.unshift();
                // System.out.println(singlyLinkedList.peek());
                // System.out.println(singlyLinkedList.contains(3));

                // differnet ways to create a String in java

        }

}
